vector<int> getLps(const string &t) {
  int n = t.size();
  vector<int> pi(n, 0);
  int lps = 0;
  for (int q = 1; q < n; q++) {
    while (lps > 0 && t[lps] != t[q]) {
      lps = pi[lps - 1];
    }
    if (t[lps] == t[q]) {
      lps += 1;
    }
    pi[q] = lps;
  }
  return pi;
}
